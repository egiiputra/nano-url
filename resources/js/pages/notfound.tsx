import { Head  } from '@inertiajs/react';
// import { type SharedData } from '@/types';
// import { usePage } from '@inertiajs/react';

export default function NotFound() {
    return (
        <h1 className='text-red-900'>404 not found</h1>
    );
}
